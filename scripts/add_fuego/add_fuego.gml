function add_fuego(x, y, a, b, hmove, vmove, intensidad){
	var fuego = {
		x : real(x),
		y : real(y),
		a : real(a),
		b : real(b),
		hmove : real(hmove),
		vmove : real(vmove),
		intensidad : real(intensidad)
	}
	return fuego
}