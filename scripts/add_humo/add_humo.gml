function add_humo(x, y, a, b, hmove, vmove, time, value, alpha){
	var humo = {
		x : real(x),
		y : real(y),
		a : real(a),
		b : real(b),
		hmove : real(hmove),
		vmove : real(vmove),
		time : real(time),
		value : real(value),
		alpha : real(alpha)
	}
	return humo
}