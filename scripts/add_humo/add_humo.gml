function add_humo(x, y, a, b, hmove, vmove, time){
	var humo = {
		x : real(x),
		y : real(y),
		a : real(a),
		b : real(b),
		hmove : real(hmove),
		vmove : real(vmove),
		time : real(time)
	}
	return humo
}