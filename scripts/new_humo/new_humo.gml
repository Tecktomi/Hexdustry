function new_humo(x, y, hmove, vmove, time, value, alpha){
	var humo = {
		x : real(x),
		y : real(y),
		hmove : real(hmove),
		vmove : real(vmove),
		time : real(time),
		value : real(value),
		alpha : real(alpha)
	}
	return humo
}